name: CI/CD - Frontend (Tests, Build, Deploy + HTTPS)

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test:
    name: Ejecutar pruebas
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run test

  deploy:
    name: Despliegue en Azure
    needs: test
    runs-on: ubuntu-latest

    env:
      # ===== Azure / ACR =====
      ACR_LOGIN_SERVER: ${{ secrets.ACR_LOGIN_SERVER }}   # p.ej. eduaotiacr.azurecr.io
      ACR_NAME:         ${{ secrets.ACR_NAME }}           # p.ej. eduaotiacr
      RG:               ${{ secrets.AZURE_RESOURCE_GROUP }}   # p.ej. doitnow-rg
      LOCATION:         ${{ secrets.AZURE_LOCATION }}         # p.ej. eastus

      # ===== Imagen =====
      IMAGE_NAME: doitnow-frontend
      IMAGE_TAG:  ${{ github.sha }}

      # ===== ACI ===== (fallback se hace en bash)
      CONTAINER_NAME: ${{ secrets.CONTAINER_NAME }}
      DNS_LABEL:      ${{ secrets.DNS_LABEL }}

      # ===== Front Door (HTTPS) =====
      AFD_PROFILE:      fd-doitnow
      AFD_ENDPOINT:     ep-doitnow
      AFD_ORIGIN_GROUP: og-frontend
      AFD_ORIGIN_NAME:  origin-frontend

    steps:
      - uses: actions/checkout@v4

      - name: Azure CLI Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Validar sesión
        run: az account show

      - name: ACR RBAC Login (docker)
        run: az acr login -n "$ACR_NAME"

      - name: Build & Push Frontend (ACR)
        run: |
          docker build --platform linux/amd64 -t $ACR_LOGIN_SERVER/$IMAGE_NAME:$IMAGE_TAG .
          docker push $ACR_LOGIN_SERVER/$IMAGE_NAME:$IMAGE_TAG

      - name: Deploy a Azure Container Instances (ACI)
        run: |
          CN="${CONTAINER_NAME:-doitnow-frontend}"
          DL="${DNS_LABEL:-doitnow-front-eduaoti}"

          az container delete -g "$RG" -n "$CN" -y || true

          az container create \
            --resource-group "$RG" \
            --name "$CN" \
            --image "$ACR_LOGIN_SERVER/$IMAGE_NAME:$IMAGE_TAG" \
            --dns-name-label "$DL" \
            --location "$LOCATION" \
            --ports 80 \
            --os-type Linux \
            --cpu 1 \
            --memory 1.5 \
            --registry-login-server "$ACR_LOGIN_SERVER" \
            --registry-username "${{ secrets.ACR_USERNAME }}" \
            --registry-password "${{ secrets.ACR_PASSWORD }}" \
            --restart-policy Always

      # ===== HTTPS con Azure Front Door =====
      - name: Verificar provider Microsoft.Cdn (debe estar Registered)
        run: |
          state=$(az provider show -n Microsoft.Cdn --query registrationState -o tsv || echo "Unknown")
          echo "Microsoft.Cdn registrationState=$state"
          if [ "$state" != "Registered" ]; then
            echo "El provider Microsoft.Cdn no está Registered. Regístralo una vez con una cuenta Owner/Contributor de suscripción."
            exit 1
          fi

      - name: Obtener FQDN/IP de ACI (frontend)
        id: aci
        run: |
          CN="${CONTAINER_NAME:-doitnow-frontend}"
          FQDN=$(az container show -g "$RG" -n "$CN" --query "ipAddress.fqdn" -o tsv || true)
          if [ -z "$FQDN" ]; then
            FQDN=$(az container show -g "$RG" -n "$CN" --query "ipAddress.ip" -o tsv)
          fi
          echo "fqdn=$FQDN" >> $GITHUB_OUTPUT
          echo "ACI Host: $FQDN"

      - name: Crear/usar Front Door Profile
        run: |
          az afd profile show -g "$RG" -n "$AFD_PROFILE" \
          || az afd profile create -g "$RG" -n "$AFD_PROFILE" --sku Standard_AzureFrontDoor

      - name: Crear/usar Endpoint público
        run: |
          az afd endpoint show -g "$RG" --profile-name "$AFD_PROFILE" -n "$AFD_ENDPOINT" \
          || az afd endpoint create -g "$RG" --profile-name "$AFD_PROFILE" -n "$AFD_ENDPOINT" --enabled-state Enabled

      - name: Crear/usar Origin Group
        run: |
          az afd origin-group show -g "$RG" --profile-name "$AFD_PROFILE" -n "$AFD_ORIGIN_GROUP" \
          || az afd origin-group create -g "$RG" --profile-name "$AFD_PROFILE" -n "$AFD_ORIGIN_GROUP"

      - name: Crear/actualizar Origin (ACI)
        run: |
          HN="${{ steps.aci.outputs.fqdn }}"
          az afd origin show -g "$RG" --profile-name "$AFD_PROFILE" \
            --origin-group-name "$AFD_ORIGIN_GROUP" -n "$AFD_ORIGIN_NAME" \
          && az afd origin update -g "$RG" --profile-name "$AFD_PROFILE" \
               --origin-group-name "$AFD_ORIGIN_GROUP" -n "$AFD_ORIGIN_NAME" \
               --host-name "$HN" --http-port 80 --https-port 443 --enabled-state Enabled \
          || az afd origin create -g "$RG" --profile-name "$AFD_PROFILE" \
               --origin-group-name "$AFD_ORIGIN_GROUP" -n "$AFD_ORIGIN_NAME" \
               --host-name "$HN" --http-port 80 --https-port 443 --enabled-state Enabled

      - name: Crear/usar Ruta (HTTPS redirect, forward HTTP)
        run: |
          az afd route show -g "$RG" --profile-name "$AFD_PROFILE" \
            --endpoint-name "$AFD_ENDPOINT" -n route-frontend \
          || az afd route create -g "$RG" --profile-name "$AFD_PROFILE" \
               --endpoint-name "$AFD_ENDPOINT" -n route-frontend \
               --origin-group "$AFD_ORIGIN_GROUP" \
               --supported-protocols Http Https \
               --https-redirect Enabled \
               --forwarding-protocol HttpOnly \
               --link-to-default-domain Enabled \
               --patterns "/*"

      - name: Mostrar URL HTTPS del Frontend
        run: |
          FD_HOST=$(az afd endpoint show -g "$RG" --profile-name "$AFD_PROFILE" -n "$AFD_ENDPOINT" --query "hostName" -o tsv)
          echo "Frontend HTTPS: https://${FD_HOST}"

      - name: Mostrar estado ACI
        run: |
          CN="${CONTAINER_NAME:-doitnow-frontend}"
          az container show -g "$RG" -n "$CN" --query "{FQDN:ipAddress.fqdn,State:instanceView.state}" -o table
